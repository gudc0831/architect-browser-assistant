Req: Track the umbrella operational gap implementation after Slice 474 and strengthen Browser Assistant production packaging readiness.
Diff: Added plans 475-482, updated the slice roadmap/current goal and success criteria, strengthened release readiness docs, added `release:build:production` and `native-host:verify-production-install`, added installer `-InstallRoot`, production install verification, and `--require-webstore` Chrome profile validation.
Why: The umbrella goal intentionally grouped the remaining operational MVP gaps, but each sub-slice still needs durable planning state, blocked-condition records, and production release gates that do not perform actual Web Store upload or signing.
Verify/Time: 2026-05-15 18:45 KST; `npm run typecheck`, `npm run lint`, `npm run test`, and `npm run release:check` passed; `npm run release:readiness -- --json --strict` passed with 13 pass, 4 expected local-production warnings, and 0 fail; `npm run release:readiness:production -- --json --strict` failed intentionally on localhost origin and missing production Web Store/signing/install-root metadata blockers.

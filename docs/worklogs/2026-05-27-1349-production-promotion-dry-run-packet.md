Req: Continue from production readiness warning cleanup with one bundled slice for the next necessary pre-promotion work, without uploading to Chrome Web Store, signing binaries, issuing certificates, committing generated native-host files, or changing user registry state.
Diff: Added `npm run release:promotion:dry-run`, a non-mutating operator packet that fingerprints the built `dist` artifact, runs production readiness, classifies blocking metadata gaps versus manual approval dependencies, and emits follow-up commands; added Node tests and README/plan documentation.
Why: Production promotion needs a reproducible artifact/readiness/dependency packet before any external approval step, while local `release:check` must continue to pass with expected localhost/generated-file warnings.
Verify/Time: `npm test` passed; synthetic production dry-run with non-secret metadata and `--allow-unsigned-native-host` passed with `ok=true`; local `npm run release:promotion:dry-run -- --json` reports expected blockers without mutation; `npm run release:check` passed | 2026-05-27 13:49 KST

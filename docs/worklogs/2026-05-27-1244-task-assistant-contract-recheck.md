Req: Treat issue tracking as task assistant records, work-summary cleanup, Review Closure Gate, and Knowledge candidate state; continue from the approved SaaS-contract-first sequence.
Diff: Switched local browser work to `codex/task-assistant-core-loop`, rechecked the existing SaaS assistant extension contract from the sibling SaaS repo, and moved the native-host unit test from Vitest to Node's built-in test runner while keeping browser/React tests on Vitest.
Why: The SaaS contract and browser foundation already exist on the task-assistant branch, so progress should validate and stabilize that implementation instead of recreating it from `main`; the native-host `.mjs` test was blocking `npm test` under Vitest despite passing Node syntax checks.
Verify/Time: `npm run typecheck`, `npm test`, `npm run lint`, and `npm run build` passed in `architect-browser-assistant`; sibling SaaS repo was inspected read-only because it has unrelated dirty changes | 2026-05-27 12:44 KST

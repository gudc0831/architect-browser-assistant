Req: `architect-browser-assistant` PLAN 기준 MVP 완료 감사 중, assistant 답변 화면에서 confidence percentage와 reason이 저장 상태 문자열로만 드러나는 누락을 보강한다.
Diff: `src/side-panel/App.test.tsx`에 React side-panel regression test를 추가하고, `src/side-panel/App.tsx`/`styles.css`에서 saved assistant record의 `confidenceScore`와 `confidenceReason`을 Answer 영역의 confidence block으로 표시한다. `plans/01-task-assistant-core-loop.md`의 신뢰도 표시 상태와 검증 로그를 갱신한다.
Why: `PLAN.md`와 Slice 01은 assistant 답변에 confidence percentage와 설명을 표시해 사용자가 근거 한계와 법적 확정 아님을 구분하도록 요구한다. 저장 완료 문구만으로는 답변 화면의 명시적 완료 기준을 충족하지 못했다.
Verify/Time: 2026-05-15 09:16-09:22 KST. Red test: `npm run test -- src/side-panel/App.test.tsx` failed because `Confidence 76%` and `confidenceReason` were not rendered. Green: targeted test passed. `npm run typecheck` passed. `npm run lint` passed. `npm run release:check` passed with 7 test files / 15 tests, production build, release readiness 11 pass / 3 expected local-production warnings / 0 fail, and native-host self-test ok. `npm run native-host:verify-generation -- --json --mock --strict` passed. React TSX Vitest was run outside the sandbox because sandbox execution mapped the test through a Windows `/@fs/D:/...` path that could not be loaded.
